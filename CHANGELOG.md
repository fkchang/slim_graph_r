# Changelog

## 0.30.0

- Add a question-first progressive diagram chooser skill, six real rendered README examples, and direct Diagram Design attribution so humans and agents can see both the available grammars and the judgment behind choosing one.
- Complete RubyGems metadata, MFA protection, extracted-standard-library dependency declarations, an isolated installed-gem quickstart check, and CI coverage through Ruby 4.0.
- Add an immutable graph storyboard, shared accessible browser player, static/reveal/step output modes, replacement and current-step semantics, and executable fan-in queue, paired policy trace, and secure paved-road examples.
- Add compact bounded renderers for those three researched motion patterns, keep controls before the figure, prevent narrow motion SVGs from expanding vertically, and expose authored targets for sequence, state, process/swimlane, and data-flow diagrams.
- Add dedicated Polar and Radar models with immutable BigDecimal values, strict type-specific JSON, exact linear radius geometry, accessible descriptions, and visible captions that reject radial-area meaning.
- Preserve polar category order and equal angles with rays and constant-size markers only; exact zero retains its spoke and labels while drawing no value ray or marker.
- Require one already-common Radar scale across 2–5 entities and 3–5 criteria; render outline-only polygons with colour-independent stroke patterns and no hidden normalization.
- Add Ruby/JSON standalone examples, optional StreamWeaver examples, CLI/gallery integration, measured layout failures, and guides. Named coverage is now 39 partial / 0 missing / 0 verified; full upstream variant parity is not claimed.

## 0.29.0

- Add bounded `:treemap` and `:sankey` charts with dedicated immutable models, strict Ruby/JSON inputs, and generated accessible conservation descriptions.
- Preserve exact unsnapped treemap shares, zero-item absence, tiny positive areas, one shared Sankey scale, fractional ribbons, strict node/stage balance, and disjoint attachment offsets without hidden repair or invented aggregation.
- Add paired standalone and StreamWeaver examples, guides, focused geometry specs, CLI/package coverage, and the acceptance sheet. Track 37 partial, 2 missing, and 0 verified types against the pinned Cathryn Lavery reference and vendored MIT notice.

## 0.28.0

- Add bounded `:bar`, `:line`, and `:scatter` charts on a shared immutable BigDecimal foundation with strict Ruby numeric types and JSON allowlists.
- Preserve exact signed and zero geometry, elapsed-day time positions, explicit line gaps, ordinal-spacing warnings, and required scatter domains without interpolation, snapping, clipping, or fake marks.
- Add plain half-even numeric formatting, visible and accessible scale metadata, deterministic constant-data reference domains, measured labels, and unsafe SVG Float conversion failures.
- Add paired Ruby/JSON and optional StreamWeaver examples, focused geometry/validation specs, CLI/package coverage, and three guides. Track 35 partial, 4 missing, and 0 verified types against the pinned upstream reference.

## 0.27.0

- Add `:wardley` with dedicated immutable component/dependency records, explicit qualitative band and visibility positions, strict Ruby/JSON identity, and generated accessible descriptions.
- Render a measured 960px map with four equal evolution bands, stacked visibility endpoint phrases, straight muted dependencies, and up to two adjacent rightward dashed movement arrows.
- Preserve 12px logical meaningful text and the shared 16/14 display scale, producing a 1280px standard SVG with focusable horizontal scrolling in narrow StreamWeaver regions.
- Reject inferred/free positions, scores, ticks, isolated components, runtime semantics, and geometric repair; protected interference raises actionable `LayoutError` without moving dots.
- Add standalone Ruby/JSON, optional StreamWeaver, gallery, CLI/package, and guide coverage with pinned Cathryn Lavery attribution. Track Wardley as the 32nd partial type: 32 partial, 7 missing, 0 verified.

## 0.26.1

- Promote meaningful metadata to 12 logical pixels in fishbone, data flow, high level, DP integration, workflow, story-map, and shared journey roles; ordinary card/content labels use the 14px body floor.
- Measure the affected cards, labels, protocol masks, legends, lanes, and journey columns around those truthful roles, allowing every affected family to use the shared 16/14 display scale.
- Compact fishbone slot cadence and spine travel while preserving exact 60-degree bones, 32px ticks, authored topology, and a 160px expansion for a third lower category.
- Keep standard affected examples at or below 1418px and the three-lower fishbone stress example at 1600px, with intrinsic min-width and focusable narrow scrolling unchanged.
- Preserve the parity ledger at 31 partial, 8 missing, and 0 fully verified types.

## 0.26.0

- Add `:fishbone` with dedicated immutable effect/category records, atomic factor scopes, strict Ruby/JSON identity, and descriptions that preserve investigative uncertainty.
- Render a measured horizontal spine into a 200px effect head, exact 60-degree category bones, and 32px factor ticks; a third lower category expands the head and viewBox together.
- Add standalone Ruby/JSON, optional StreamWeaver, gallery, paired preview, CLI/package coverage, and a fishbone guide with pinned Cathryn Lavery attribution.
- Track Fishbone as the 31st partial type; the release has 31 partial, 8 missing, and 0 fully verified types.

## 0.25.0

- Add `:loop` with dedicated immutable hub/station/write-back records, a mandatory authored clockwise cycle, strict Ruby/JSON identity, and generated accessible descriptions.
- Render measured 160×64 station cards around a 200×104 shared-state hub, true same-radius clockwise arcs including explicit closure, and dashed edge-clipped radial write-backs with a 6px hub gap.
- Reject inferred spokes/closure, missing or duplicate relations, skips, branches, alternate directions, multiple hubs or focal stations, and custom geometry.
- Add standalone Ruby/JSON, optional StreamWeaver, gallery, paired preview, CLI/package coverage, and the loop guide with pinned Cathryn Lavery attribution.
- Track Loop as the 30th partial type; the release has 30 partial, 9 missing, and 0 fully verified types.

## 0.24.0

- Add `:venn` with dedicated immutable set/intersection records, complete named two- or three-set topology, strict Ruby/JSON identity, and generated accessible descriptions.
- Render fixed equal-circle templates with external set labels, fixed clear pair/triple regions, compound low-opacity tints, and one optional clipped author-selected focal overlap; area and population remain explicitly nonquantitative.
- Register Venn’s logical 14px meaningful minimum with the readable display policy, yielding a 16px physical minimum and base body text at 16px.
- Add standalone Ruby/JSON, optional StreamWeaver, gallery, paired preview, CLI/package coverage, and the Venn guide with pinned Cathryn Lavery attribution.
- Track Venn as the 29th partial type; the release has 29 partial, 10 missing, and 0 fully verified types.

## 0.23.0

- Add `:quadrant` with dedicated immutable axis/item records, exact authored qualitative x/y positions, strict Ruby/JSON identity, and generated accessible descriptions.
- Render a measured 720×480 2×2 plot with literal endpoint phrases, exact linear mapping, one optional authored focal point, clear same-quadrant labels, and a fixed qualitative-position caption; unplaceable labels fail instead of moving points.
- Register the logical 8px axis role with the shared 1.5× readable display policy, yielding a 12px physical minimum and body text above 16px.
- Add standalone Ruby/JSON, optional StreamWeaver, gallery, paired preview, CLI/package coverage, and the quadrant guide with pinned Cathryn Lavery attribution.
- Track Quadrant as the 28th partial type; the release has 28 partial, 11 missing, and 0 fully verified types.

## 0.22.0

- Add `:uml_class` with dedicated immutable class/member/relation records, literal Ruby and strict JSON inputs, and generated accessibility descriptions.
- Render natural attribute/operation compartments, all six relation kinds and legend entries, target-end hollow triangles, declared-owner diamonds, plain association lines with exact endpoint multiplicities, and dependency-only open arrows.
- Register UML’s logical 8px grammar role with the shared 1.5× readable display policy, yielding a 12px physical minimum while preserving measured geometry and narrow-screen horizontal scrolling.
- Add standalone Ruby/JSON, optional StreamWeaver and paired preview examples, CLI/package coverage, and the UML class guide with pinned Cathryn Lavery attribution.
- Track UML Class Diagram as the 27th partial type; the release has 27 partial, 12 missing, and 0 fully verified types.

## 0.21.1

- Render every diagram at a type-aware physical scale so ordinary text is about 16px and each type's smallest meaningful metadata is at least 12px, preserving the complete SVG geometry rather than enlarging text inside fixed boxes.
- Raise database-schema metadata from 8px to 9px authored units and use the same constants for layout measurement and SVG drawing; at the schema's 4/3 display scale names render at 16px and metadata at 12px.
- Replace the arbitrary 86% shrink allowance with intentional horizontal overflow at the readable size in standalone, StreamWeaver canvas/read, and HTML-export paths.

## 0.21.0

- Add `:db_schema` with dedicated immutable table, column, and foreign-key records, a compact nested Ruby DSL, strict equivalent JSON, and generated accessibility descriptions.
- Render measured table cards, fixed 24px column rows, literal SQL types/constraint chips/indexes, explicit schema groups, exact row-level FK endpoints, symmetric shared-row ports, rounded orthogonal routes, and masked deletion-action labels.
- Preserve every authored cascade and tint its dependent source-table header while keeping referenced parents and other actions neutral; unplaceable content raises actionable layout errors.
- Add standalone Ruby/JSON, optional StreamWeaver and paired preview examples, CLI/package coverage, and the database-schema guide with pinned Cathryn Lavery attribution.
- Track database schema as the 26th partial type; the release has 26 partial, 13 missing, and 0 fully verified types.

## 0.20.0

- Add `:er` with dedicated immutable entity, field, and relationship records, a compact nested Ruby DSL, strict equivalent JSON, and generated accessibility descriptions.
- Render natural-height entity cards, literal `#`/`→` key classifications, neutral rounded orthogonal relationships, distinct ports, exact authored endpoint cardinalities, and masked labels with actionable layout failures.
- Add standalone Ruby/JSON, optional StreamWeaver and preview examples, CLI/package coverage, and the entity-relationship guide with pinned Cathryn Lavery attribution.
- Track ER as the 25th partial type; the release has 25 partial, 14 missing, and 0 fully verified types.

## 0.19.0

- Add `:dp_security_matrix` with dedicated immutable role, component, and permission records, a compact Ruby DSL, strict equivalent JSON, connector-free measured SVG, exact generated accessibility text, and actionable layout failures.
- Require every role/component permission explicitly. Authored `unknown` remains distinct from denial and from invalid omission; deterministic level labels never imply a permission.
- Add standalone Ruby/JSON and optional StreamWeaver examples, the access-matrix guide, CLI/package coverage, and all four styles in light and dark modes.
- Track DP security matrix as the 24th partial type against the pinned Cathryn Lavery Diagram Design reference; the release has 24 partial, 15 missing, and 0 fully verified types.

## 0.18.0

- Add a bounded DP-integration model with strict Ruby/JSON parity, platform zones, explicit protocol wires, layer-wide services, accessible descriptions, and faithful-layout errors.
- Add measured geometry for side cards, platform bands, neutral ordinary wires, protocol clearance, distinct boundary ports, and one footer-card strip.
- Track DP integration as the 23rd partial type against the pinned Cathryn Lavery Diagram Design reference; the release has 23 partial, 16 missing, and 0 fully verified types.

## 0.17.0

- Add a dedicated bounded Data Flow model with immutable Ruby records, strict JSON parity, checkout CLI/library rendering, optional StreamWeaver integration, measured role × stage geometry, and accessible descriptions.
- Add declared transfer payloads, explicit ordinary/trigger/focal/publish handoffs, one linked focal claim, fixed 152×72 transfer cards, orthogonal routes, and actionable faithful-layout errors.
- Track Data Flow as the 22nd partial type against the pinned Cathryn Lavery Diagram Design reference; the release has 22 partial, 17 missing, and 0 fully verified types.

## 0.16.0

- Add dedicated bounded User Journey and User Story Map models with immutable Ruby DSLs, strict JSON parity, CLI/library rendering, optional StreamWeaver integration, measured layouts, and accessible descriptions.
- Add ordinal sentiment journeys with a unique trough and trough-only pain markers; add narrative story maps with release slices, one non-final cut, authored estimate/ticket metadata, and one optional risk story.
- Track User Journey Map and User Story Map as the 20th–21st partial types against the pinned Cathryn Lavery Diagram Design references; the release has 21 partial, 18 missing, and 0 fully verified types.

## 0.15.0

- Add dedicated bounded Gantt and Kanban planning-board models with immutable Ruby DSLs, strict JSON parity, CLI/library rendering, and accessible descriptions.
- Add exact elapsed-day Gantt geometry with complete Gregorian dates, half-open task bars, declaration-order phase rows, milestone tracks, static markers, and actionable faithful-layout errors.
- Add ordered Kanban columns/cards with neutral/default, blocked, waiting, and done states, optional ticket/owner metadata, focal-ring emphasis, derived counts, honest WIP violations, and actionable geometry errors.
- Track Gantt Chart and Kanban Board as the 18th–19th partial types against the pinned Cathryn Lavery Diagram Design references; the release has 19 partial, 20 missing, and 0 fully verified types.

## 0.14.0

- Add dedicated bounded Swimlane and Process models with immutable Ruby DSLs, strict JSON parity, horizontal actor-lane geometry, and explicit forward handoffs.
- Add process lane keys, tools, payload codes, independent focal stage/operation slots, one optional labelled return trigger, accessible descriptions, CLI/library rendering, and optional StreamWeaver examples.
- Track Swimlane and Process as the 16th–17th partial types against the pinned Cathryn Lavery Diagram Design references; the release has 17 partial, 22 missing, and 0 fully verified types.

## 0.13.0

- Add dedicated bounded Pyramid / Funnel and Medallion models with immutable Ruby DSLs, strict JSON parity, CLI/library rendering, and accessible descriptions.
- Preserve honest measured widths, equal-count plateaus, zero tails, outside leaders, fixed medallion card/arc geometry, optional final archives, semantic concern colors, and 0–2 write paths with actionable faithful-layout errors.
- Track Pyramid / Funnel and Medallion as the 14th–15th partial types against the pinned Cathryn Lavery Diagram Design references; the release has 15 partial, 24 missing, and 0 fully verified types.

## 0.12.0

- Add dedicated bounded Tree / Hierarchy, Nested Containment, and Layer Stack models with immutable Ruby DSLs and faithful type-specific layouts.
- Add strict recursive/array JSON parity, standalone and optional StreamWeaver examples, CLI rendering, accessible descriptions, and actionable layout errors for the three hierarchy families.
- Add optional tree focal treatment, regular nested scope insets with automatic innermost focus, and contiguous numeric or unique semantic layer indices with axis direction metadata.
- Track these as the 11th–13th partial types against the pinned Cathryn Lavery Diagram Design references; the release has 13 partial, 26 missing, and 0 fully verified types.

## 0.11.0

- Add a dedicated bounded high-level data-stack diagram with source phases, component phases, an explicit focal component, orchestration targets, and concern-paired crosscuts.
- Add strict Ruby/JSON parity, both CLI forms, optional StreamWeaver loading, source-type accessibility, deterministic horizontal geometry, and actionable faithful-layout errors.
- Track High-Level as the tenth partial type against the pinned Cathryn Lavery Diagram Design reference; full upstream parity remains open.

## 0.10.0

- Add a bounded IT current-state diagram with horizontal phases, named systems, external and pain-point states, labelled handoffs, and cross-cutting services.
- Add strict Ruby/JSON parity, standalone and optional StreamWeaver authoring, both CLI forms, accessible descriptions, curated semantic legends, and faithful-layout errors.
- Track IT current-state as the ninth partial type against the pinned Cathryn Lavery Diagram Design reference; full upstream parity remains open.

## 0.9.0

- Add a dedicated bounded deployment DSL with zone containment, typed infrastructure nodes, positive replica badges, versioned artifact chips, and protocol/port network paths.
- Add strict Ruby/JSON parity, standalone and optional StreamWeaver authoring, CLI rendering, accessible deployment descriptions, and faithful-layout errors for unplaceable paths or labels.
- Track deployment as the eighth partial type against the pinned Cathryn Lavery Diagram Design reference; full upstream parity remains open.

## 0.8.0

- Add a dedicated bounded dependency-graph DSL with `dependency`, `external_dependency`, and dependent-to-requirement `depends_on` relationships.
- Add strict Ruby/JSON parity, computed fan-in badges, internal/external/leaf treatments, one validated real cycle with an outside accent lane, accessible descriptions, CLI rendering, and explicit faithful-layout failures.
- Track dependency graphs as the seventh partial type against the pinned Cathryn Lavery Diagram Design reference; full upstream parity remains open.

## 0.7.0

- Add a dedicated bounded state-machine DSL with immutable states, named transitions, entry/final markers, lifecycle validation, and measured curved routes for right/down layouts.
- Add strict Ruby/JSON parity, StreamWeaver and standalone examples, CLI rendering, accessible state/transition descriptions, and actionable `LayoutError` failures for routes or labels that cannot be laid out clearly.
- Track state machines as the sixth partial type against the pinned Cathryn Lavery Diagram Design reference; full upstream parity remains open.

## 0.6.0

- Add org-chart ownership semantics with `owner`, `invoke`, `scope`, `detail`, and explicit `unavailable` state while preserving generic org-chart nodes.
- Add escalation and approval rules rendered as accessible footer callouts, with strict owner-reference validation and bounded topology/layout budgets.
- Add strict Ruby/JSON parity for org ownership fields and rule arrays, plus standalone Ruby, JSON, and StreamWeaver examples and an [org-chart guide](docs/org-charts.md).

## 0.5.1

- Fix 0.5.0's 48px marker adjustment, which distorted clustered dates and could misplace out-of-order inputs. Preserve exact linear date positions and sort chronologically without mutating the input.
- Require complete Gregorian YYYY-MM-DD dates; remove environment-dependent partial-date guessing. Group simultaneous events at one marker while retaining every event.
- Size callouts from their content, use collision-free label rows, route leaders around text, and keep ticks readable. Reject indistinguishable dates instead of changing their positions.
- Validate scale changes through `with` and add the CLI `--scale` override. Mark ordered timelines explicitly as non-quantitative.
- Add regression coverage for date ratios, sorting, simultaneous events, leap days, single dates, long labels, invalid options and dense inputs.

## 0.5.0

- Add honest date-scaled timelines with elapsed spacing, axis ticks and alternating event labels.
- Add `scale: :date`, `:ordered`, and `:auto`; date mode requires every event date to parse as YYYY-MM-DD, while ordered mode explicitly preserves caption-like milestones.
- Keep timeline dates in accessible descriptions and exports. Update JSON, CLI and examples for the same scale semantics.
- Initial date-axis evidence was incomplete: the marker-spacing adjustment distorted elapsed intervals. Corrected in 0.5.1.

## 0.4.0

- Add scoped sequence activation bars, including nested control and explicit sibling-scope boundaries.
- Add alt/branch, opt and loop frames with guard/divider spacing and accessible control descriptions. Loop describes repetition and evaluates its Ruby body once.
- Distinguish synchronous calls, returns, async notifications and headline success messages. Add reply/notify helpers; existing dashed:true sequence messages become filled-head returns.
- Support nested sequence steps in strict JSON, using the same immutable model as Ruby and the same CLI/StreamWeaver rendering paths.
- Move self-message labels beside their loops and keep long message labels between lifelines.
- Enforce sequence-specific bounds: five participants, twelve messages, one alt or two opt/loop frames, no nested frames, and three activation levels per actor. Larger previously accepted flat sequences now require splitting.

## 0.3.0

- Add real flowchart diamonds, start/finish terminators, and explicit merge junctions. Size each shape for its labels and attach arrows to its actual boundary.
- Require labeled decision exits (at most three), and validate start/finish/merge topology. JSON supports the same kinds. This intentionally rejects previously accepted ambiguous flowcharts.
- Place simple Yes/No branches toward their conventional destinations; route other guards through distinct ports.
- Separate parallel graph strokes, keep label masks eight pixels clear of arrows, and add crossing hops. Refuse cramped routes/crossings that cannot be represented faithfully.
- Add a full flowchart example and geometry/semantic RSpec evidence. Full Diagram Design parity remains in progress.

## 0.2.0

- Add `slimgraph render` for Ruby and strict JSON documents, SVG/HTML output, stdin/stdout, named styles, and mode overrides.
- Preserve existing output files by default; publish file output atomically and require `--force` for replacement.
- Add editorial, ruby, blueprint, and mono styles independently from light/dark/auto mode. Keep editorial as the default. Adjust editorial accent colors to meet the 4.5:1 text-contrast target on tinted surfaces.
- Measure preset heading fonts before wrapping; add immutable `diagram.with(style:, theme:)` presentation overrides.
- Separate standalone examples from StreamWeaver examples. Add core-only dependency configuration, isolated package verification, and GitHub Actions core/integration jobs.
- Draft an intent-first README with the accepted printing-press artwork and actual rendered example output.

This release establishes adoption and presentation foundations. The five existing types still have documented gaps against the full 39-type Diagram Design parity target.

## 0.1.0

- Initial Ruby DSL, five diagram types, SVG/HTML rendering, and optional StreamWeaver component.
